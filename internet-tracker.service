[Unit]
Description=Internet Connection Uptime Tracker
After=network.target

[Service]
Type=simple
User=vintello
WorkingDirectory=<<host_folder>>
ExecStart=<<host_folder>>/venv/bin/python3 <<host_folder>>/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target